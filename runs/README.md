# Runs

This folder stores deterministic run manifests (run.json) and input/output bundles.

Each run directory should include:
- inputs/
- outputs/
- run.json (hashes, versions, pricing source, tax profile)
